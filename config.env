# 🚀 远程开发环境 - 简化配置
# 作者: Zhang-Jingdian (2157429750@qq.com)
# 创建时间: 2025年7月14日

# =============================================================================
# 核心配置 - 这是唯一需要的配置文件
# =============================================================================

# 远程服务器配置
SSH_ALIAS=remote-server
REMOTE_HOST=192.168.0.105
REMOTE_USER=zjd
REMOTE_PROJECT_PATH=/home/zjd/workspace
SSH_PORT=22

# 本地配置 - 使用独立的work目录作为工作空间
LOCAL_PATH=./work
REMOTE_PATH=/home/zjd/workspace
SYNC_EXCLUDE=".git,node_modules,*.log,*.tmp,.DS_Store,__pycache__"

# 服务配置
WEB_PORT=8080
# API服务端口
API_PORT=5001
DOCKER_SERVICE_NAME=dev-env

# 同步配置
WATCH_INTERVAL=1
SYNC_DIRECTION=to-remote
AUTO_RESTART=true

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=dev.log

# 高级配置（可选）
USE_COMPRESSION=true
SSH_TIMEOUT=30
MAX_RETRIES=3

# Docker配置
DOCKER_IMAGE=python:3.11-slim
DOCKER_NETWORK=dev-network

# 代理配置（如果需要）
HTTP_PROXY=
HTTPS_PROXY=

# =============================================================================
# 自动检测配置（无需修改）
# =============================================================================
PLATFORM=$(uname -s)
ARCH=$(uname -m) 