# 远程服务器配置
REMOTE_HOST=192.168.0.105
REMOTE_USER=zjd
REMOTE_PATH=/home/zjd/workspace

# 本地配置
LOCAL_PATH=./work

# 同步配置
SYNC_EXCLUDE=".git,node_modules,*.log,*.tmp,.DS_Store,__pycache__,.venv,dist,build,coverage,.pytest_cache"

# 开发工具配置
DEV_SCRIPT=./dev
DOCKER_DIR=./docker
LOGS_DIR=./docker/logs

# Docker配置
CONTAINER_NAME=remote-dev-env
DOCKER_COMPOSE_FILE=./docker/docker-compose.yml

# 开发环境配置
PYTHON_VERSION=3.11
NODE_VERSION=18

# 端口配置
PYTHON_PORT=5000
NODE_PORT=3000
DJANGO_PORT=8000
DEBUG_PORT=9000

# 性能配置
SYNC_INTERVAL=5
WATCH_ENABLED=true
AUTO_SYNC=true

# 代理配置
HTTP_PROXY=socks5://127.0.0.1:7897
HTTPS_PROXY=socks5://127.0.0.1:7897
NO_PROXY=localhost,127.0.0.1 